<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.Seat_mybatis">	
	<select id="selectSeatList" parameterType="String" resultType="SeatDto">
		select * from seat where screen_code=#{screen_code}
	</select>
</mapper>