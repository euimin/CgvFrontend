<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="mybatis.Theater_mybatis">	
	<select id="selectRegionList" resultType="String">
		select region from (select distinct region, regionid from theater order by regionid)
	</select>
	<select id="selectTheaterListByRegion" parameterType="String" resultType="String">
		select name from theater where region=#{region} order by name asc
	</select>
</mapper>